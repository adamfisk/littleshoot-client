package org.lastbamboo.client.services.download;

import junit.framework.TestCase;

/**
 * Stubbed out test for checking the headers returned from the local server.
 */
public class RunningServerTest extends TestCase
    {

    public void testDownload() throws Exception
        {
        /*
        final HttpClient client = new HttpClient();
        final HeadMethod method = new HeadMethod(
            "http://127.0.0.1:8107/api/downloadStream/spiderman_trailer.mp4?uri=urn%3Asha1%3AQP37EAGPULXVJGEUSJ7XLJ4SA7EA2XRA&noCache=1170116083571");
        client.executeMethod(method);
        final Header[] headers = method.getResponseHeaders();
        for (int i = 0; i < headers.length; i++)
            {
            System.out.println(headers[i]);
            }
            */
        }
    }
