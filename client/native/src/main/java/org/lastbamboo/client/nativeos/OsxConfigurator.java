package org.lastbamboo.client.nativeos;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * Performs any custom configuration for OSX.
 */
public class OsxConfigurator implements NativeConfigurator
    {

    private static final Logger LOG = LoggerFactory.getLogger(OsxConfigurator.class);
    
    public void configure()
        {
        }

    }
